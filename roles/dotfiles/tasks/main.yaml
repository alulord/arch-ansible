---
- name: Clone dotfiles to user home
  become_user: "{{ user.name }}"
  become: yes
  git:
    repo: "{{ dotfiles.url }}"
    dest: /home/{{ user.name }}/.dotfiles.git
    bare: yes
    key_file: /home/{{ user.name }}/.ssh/id_rsa
  tags:
    - dotfiles

- name: Checkout dotfiles
  become_user: "{{ user.name }}"
  become: yes
  command: git --git-dir=/home/{{ user.name }}/.dotfiles.git --work-tree=/home/{{ user.name }} checkout --force
  tags:
    - dotfiles

#- name: Update dotfiles submodules
#  become_user: "{{ user.name }}"
#  become: yes
#  command: git --git-dir=/home/{{ user.name }}/.dotfiles.git --work-tree=/home/{{ user.name }} submodule --init update
#  tags:
#    - dotfiles

#- name: Update betterlockscreen cache
#  become: yes
#  become_user: "{{ user.name }}"
#  command: betterlockscreen -u .local/share/backgrounds/fight.jpg

- name: Clone Oh My ZSH repo for user.
  become: yes
  become_user: "{{ user.name }}"
  git:
    repo: "https://github.com/ohmyzsh/ohmyzsh"
    dest: "/home/{{ user.name }}/.oh-my-zsh"
    depth: 1

- name: Add zsh autosuggestions plugin
  become: yes
  become_user: "{{ user.name }}"
  git:
    repo: "https://github.com/zsh-users/zsh-autosuggestions"
    dest: "/home/{{ user.name }}/.oh-my-zsh/custom/plugins/zsh-autosuggestions"
    depth: 1

- name: Add zsh fast syntaxt highlighting plugin
  become: yes
  become_user: "{{ user.name }}"
  git:
    repo: "https://github.com/zdharma/fast-syntax-highlighting.git"
    dest: "/home/{{ user.name }}/.oh-my-zsh/custom/plugins/fast-syntax-highlighting"
    depth: 1

- name: Add powerlevel10k zsh theme
  become: yes
  become_user: "{{ user.name }}"
  git:
    repo: "https://github.com/romkatv/powerlevel10k.git"
    dest: "/home/{{ user.name }}/.oh-my-zsh/custom/themes/powerlevel10k"
    depth: 1
- name: Link zsh aliases plugin
  become: yes
  become_user: "{{ user.name }}"
  file:
    src: /home/{{ user.name }}/.local/share/scripts/aliases
    path: /home/{{ user.name }}/.oh-my-zsh/custom/plugins/aliases
    state: link

- name: Remove README from home
  file:
    path: /home/{{ user.name }}/README.md
    state: absent
